# dsh-mesh 实验记录

> 原则：没实测不声称。真进程实验装置 mesh-experiment.mjs，判决按原始数据核对。

## EXP-1 故障隔离（真进程对照）

3 worker 分片流水线（alpha 0/3 · beta 1/3 lazy · gamma 2/3）× 40 任务。beta 正持锁处理任务 1 时 `kill -9`：

- 收养扫描：**1 个任务三证据判死（agent-dead）→ dead-letter 重派** → beta-reborn 同分片接手
- 最终 **40/40 完成**；alpha/gamma 日志零错误（全程零感知）
- 对照组（同生共死架构）：无隔离则 workflow 全停

## EXP-2 死循环检测

hang-agent 占锁后忙循环卡死（事件循环真阻塞 → 心跳停）→ 租约超时（leaseMs=2000）判死 → 任务重派 → good-agent 6/6 完成。判决：死循环 agent 不阻塞其余 agent——隔离生效。

## EXP-3 消息抗崩（对照组）

10 条消息在磁盘（文件即消息）。对照组（RPC 架构）：消息在内存管道，消费者崩溃 → 未消费消息永久丢失（0/10）。Mesh：崩溃后磁盘消息 **10/10 可恢复**。

## 装置教训（实验过程抓出）

1. 竞争模型失衡（快 worker 抢光任务 → kill 时目标 worker 无锁可持）→ 改分片流水线（每个 worker 独立份额，中游崩溃剧本真实）
2. worker 完工未释放锁 → 残留锁被 sweep 误判超时 → 完工必须 release（协议纪律）
3. `sleep` 卡死不阻塞事件循环（心跳仍在跳，永不超时）→ 死循环模拟必须忙循环真阻塞（真实死循环语义）
4. done 计数口径混入 result 文件 → 计数过滤修正
